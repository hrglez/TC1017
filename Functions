#include <iostream>
using namespace std;

int addition (int x,int y)
  {
  int a;
  a=x+y;
  return a;
  }
int subtraction (int x,int y)
  {
  int s;
  s=x-y;
  return s;
  }

int main()
{
cout<< subtraction(8,3)<<endl<<addition(10,2);
return 0;
}
